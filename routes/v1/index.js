'use strict';

var express = require('express');
var router = express.Router();

var joi = require('../../config/npm.config').joi;

router.get('/api', function(req, res, next) {
	
});

module.exports = router;